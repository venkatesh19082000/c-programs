#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,sum,b,i;
    scanf("%d", &n);
    sum=0;
    for(i=0;i<5;i++)
    {
    b=n%10;
    sum+=b;
    n=n/10;
    }
    printf("%d",sum);
    return 0;
}
